# Page Pulse — Web Audit Tool

A lightweight, stateless web page audit micro-service and dashboard built on Cloudflare Pages Functions and React.

## Quickstart & Local Setup

```bash
# Install dependencies
npm install

# Run unit test suite
npm run test

# Start frontend development server
npm run dev

# Preview full stack (Function + React) with Wrangler locally
npm run preview
```
